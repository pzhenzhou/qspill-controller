---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: qspillpolicies.scheduling.qspill.io
spec:
  group: scheduling.qspill.io
  names:
    kind: QSpillPolicy
    listKind: QSpillPolicyList
    plural: qspillpolicies
    shortNames:
    - qsp
    singular: qspillpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceQueue
      name: Source Queue
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.sourceQueueUtilization
      name: Utilization
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: QSpillPolicy is the Schema for the qspillpolicies API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: QSpillPolicySpec defines the desired state of QSpillPolicy
            properties:
              reclaimPolicy:
                properties:
                  gracePeriod:
                    default: 5m
                    type: string
                  strategy:
                    default: Graceful
                    enum:
                    - Immediate
                    - Graceful
                    type: string
                type: object
              sourceQueue:
                type: string
              spillTargets:
                items:
                  properties:
                    maxSpillCapacity:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        x-kubernetes-int-or-string: true
                      type: object
                    namespace:
                      type: string
                    priority:
                      default: 50
                      format: int32
                      type: integer
                    queueName:
                      type: string
                  required:
                  - queueName
                  type: object
                type: array
              spillTrigger:
                properties:
                  evaluationPeriod:
                    default: 60s
                    type: string
                  utilizationThreshold:
                    pattern: ^(0(\.\d+)?|1(\.0+)?)$
                    type: string
                required:
                - utilizationThreshold
                type: object
            required:
            - sourceQueue
            - spillTargets
            - spillTrigger
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentSpillTargets:
                items:
                  properties:
                    namespace:
                      type: string
                    queueName:
                      type: string
                    spillStartTime:
                      format: date-time
                      type: string
                    spilledCapacity:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        x-kubernetes-int-or-string: true
                      type: object
                  required:
                  - namespace
                  - queueName
                  - spillStartTime
                  type: object
                type: array
              lastTransitionTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              sourceQueueUtilization:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
